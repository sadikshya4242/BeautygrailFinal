{
  "name": "playwright-cucumber-bdd",
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js",
    "test:bdd": "cucumber-js --require ts-node/register --require tests/steps/**/*.ts --require support/**/*.ts"

  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.4.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.9.2"
  }
}
